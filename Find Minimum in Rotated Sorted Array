class Solution {
    public int findMin(int[] nums) {
        int minValue= nums[0];
        for(int i =0 ; i< nums.length ; i++){
            minValue = Math.min(minValue , nums[i]);
        }
        return minValue;
    }
}
